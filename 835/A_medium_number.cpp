#include <bits/stdc++.h>

#include <algorithm>

using namespace std;

int main(int argc, char *argv[]) {
  int tt;
  cin >> tt;

  while (tt--) {
    int a, b, c;
    cin >> a >> b >> c;

    int max
  }

  return 0;
}
