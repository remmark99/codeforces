#include <bits/stdc++.h>

#include <algorithm>
#include <cctype>

using namespace std;

void solve() {
  int n;
  cin >> n;

  multiset<int> ms;

  uint64_t res = 0;
  for (int i = 0; i < n; i++) {
    int temp;
    cin >> temp;

    if (temp == 0) {
      if (ms.size() == 0) {
        continue;
      } else {
        ms.
      }
    }
  }
}

int main(int argc, char *argv[]) {
  ios_base::sync_with_stdio(false);
  cin.tie(0);

  int tt;
  cin >> tt;

  while (tt--) {
    solve();
  }

  return 0;
}
